
---
interface Props {
    className?: string;
    label: string;
    href: string
}
const { className, href, label } = Astro.props;
---

<a href={href} class="group transition-all duration-300 ease-out">
    <span
        class:list={[
            "border-primary border-2 px-4 py-2 rounded-full bg-primary/50 text-text-light",
            "transition-all duration-300 ease-out",
            "group-hover:shadow-[0_0_20px_rgba(var(--color-primary-rgb),0.6)]",
            "group-hover:bg-primary/70",
            "group-hover:scale-105",
            className,
        ]}>{label}</span
    >
</a>
